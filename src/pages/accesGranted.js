import React from 'react';
import { Link } from 'gatsby';

const AccessGranted = () => (
    <main>
        <h4>Access Granted !!</h4>
        <Link to = "/ui/">Next</Link>
    </main>
)

export default AccessGranted